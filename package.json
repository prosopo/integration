{
  "name": "integration",
  "version": "0.0.0",
  "stableVersion": "0.0.0",
  "author": "Prosopo",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/prosopo-io/integration.git"
  },
  "scripts": {
    "clean": "find . -name \"node_modules\" -type d -prune -exec rm -rf '{}' + && find . -name \"build\" -type d -prune -exec rm -rf '{}' + && rm -f yarn.lock",
    "build": "yarn run build:contract && yarn run build:provider && yarn run build:procaptcha && yarn run build:procaptcha-react",
    "build:contract": "yarn workspace @prosopo/contract run clean && yarn workspace @prosopo/contract run build",
    "build:provider": "yarn workspace @prosopo/provider run clean && yarn workspace @prosopo/provider run build",
    "build:procaptcha": "yarn workspace @prosopo/procaptcha run clean && yarn workspace @prosopo/procaptcha run build",
    "build:procaptcha-react": "yarn workspace @prosopo/procaptcha-react run clean && yarn workspace @prosopo/procaptcha-react run build",
    "build:client-example": "yarn workspace client-example run build",
    "setup": "yarn workspace @prosopo/provider run setup",
    "start": "yarn workspace @prosopo/provider run start",
    "fix-typescript-references": "utils-typescript-references",
    "test": "yarn run test:mock",
    "test:mocha": "yarn workspace @prosopo/provider run test:mocha",
    "test:mock": "./scripts/test.sh",
    "lint": "yarn workspace @prosopo/provider run eslint --fix src && yarn workspace @prosopo/contract run eslint --fix src",
    "populate-data": "yarn workspace @prosopo/provider run populate-data",
    "git-sync": "git submodule foreach -q --recursive 'git switch $(git config -f $toplevel/.gitmodules submodule.$name.branch || echo master)'"
  },
  "private": true,
  "engines": {
    "node": ">=8.2.0"
  },
  "packageManager": "yarn@3.2.1",
  "workspaces": [
    "client-example",
    "packages/*"
  ],
  "resolutions": {
    "@polkadot/api": "8.7.1",
    "@polkadot/api-contract": "8.7.1",
    "@polkadot/api-derive": "8.7.1",
    "@polkadot/api-augment": "8.7.1",
    "@polkadot/apps-config": "0.116.1",
    "@polkadot/keyring": "9.4.1",
    "@polkadot/rpc-provider": "8.7.1",
    "@polkadot/types": "8.7.1",
    "@polkadot/networks": "9.4.1",
    "@polkadot/util": "9.4.1",
    "@polkadot/x-fetch": "9.4.1",
    "@polkadot/x-global": "9.4.1",
    "@polkadot/x-ws": "9.4.1",
    "@polkadot/util-crypto": "9.4.1",
    "rxjs": "7.5.5",
    "bn.js": "4.12.0"
  },
  "devDependencies": {
    "@rushstack/eslint-patch": "^1.0.6",
    "@typescript-eslint/parser": "^5.14.0"
  },
  "description": "Integrates protocol and provider for development purposes",
  "bugs": {
    "url": "https://github.com/prosopo-io/integration/issues"
  },
  "homepage": "https://github.com/prosopo-io/integration#readme",
  "main": ".eslintrc.js",
  "keywords": [],
  "license": "ISC"
}
